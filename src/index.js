import React from 'react';
import ReactDOM from 'react-dom';
import TodoList from './submitForm/TodoList';

ReactDOM.render(
  <React.StrictMode>
    <TodoList />
  </React.StrictMode>,
  document.getElementById('root')
);



